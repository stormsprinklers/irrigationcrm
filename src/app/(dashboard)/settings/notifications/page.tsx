import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function SettingsNotificationsPage() {
  return <PlaceholderPage breadcrumb={["Settings", "Notifications"]} title="Notifications" />;
}
