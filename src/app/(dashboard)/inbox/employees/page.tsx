import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function InboxEmployeesPage() {
  return <PlaceholderPage breadcrumb={["Inbox", "Employees"]} title="Employees" />;
}
